# Да? Пизда!

Telegram bot на aiogram.

## Локальный запуск

1. Скопируйте `.env.example` в `.env`.
2. Укажите токен Telegram-бота в `BOT_TOKEN`.
3. Запустите:

```bash
task run
```

## Проверки

```bash
task test
task lint
```
